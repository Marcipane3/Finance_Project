# Secrets
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.ruff_cache/

# Distribution
build/
dist/
*.egg-info/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data caches (large, recomputable)
track_b/data/cache/
track_a/data/cache/
*.parquet
*.feather

# Generated reports (keep the structure, not the output)
track_b/output/reports/*.md
!track_b/output/reports/.gitkeep

# Holdings files (personal financial data — never commit).
# These are the real paths the pipelines read (live.py / holdings.py).
# Personal positions also live only in the cockpit browser (localStorage).
track_b/holdings.csv
track_a/holdings.csv
track_b/data/holdings.csv
track_a/data/holdings.csv

# Logs
*.log
logs/

# Old thesis artifacts (already lived elsewhere)
*.ipynb_checkpoints/

.env.txt
